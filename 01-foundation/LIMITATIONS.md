# Environment Limitations

Additional Notes:
- UserLAnd restricts access to /proc and netlink sockets
- ifconfig provides limited but usable interface information
- nano editor fails due to non-interactive terminal (TTY limitation)
- vim works with warnings due to non-TTY environment
